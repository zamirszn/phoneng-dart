name: phone_ng

description: Zero-dependency Dart library for parsing, validating, normalizing, and formatting Nigerian phone numbers
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.11.5

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
